# description
Data selector.  Allows you to query and modify data structures using selector strings.
Replacement for yq, jq. Works with json, yaml, toml, xml, csv. No runtime dependencies.

# see also
duckdb
fx
jiq

# install
brew install dasel

# docs
github.com/tomwright/dasel

# select
echo '{"name": "Tom" } | dasel -r json - '.name' "Tom"

# json to yaml
echo '{"name": "Tom" } | dasel -r json -w yaml name: Tom

# put
echo '{"name": "Tom" } | dasel put string -r json '.email' 'contact@tomwwrigth.me'

# delete
echo '{ "email", "contact@tomwright.me", "name" : "Tom" }' dasel  delete -r json '.email'

# features
query/select
update
create
linux/mac/windows
docker support
faster than jq/yq

# nested structures
$ dasel -w csv -f /Users/tom/tmp/dasel/287/data.json '.data.search.nodes.all().comments.nodes.all().mapOf(number,parent(3).number,title,parent(3).title,author,parent(3).author.login,createdAt,parent(3).createdAt,commentAuthor,author.login,commentText,bodyText,commentTime,createdAt).merge()'
$ cat t.taxonid.stats.json  | dasel -w csv -r json .'all().reports.[1].mapOf(acc,accession,b,assembly_info.assembly_name,c,assembly_info.assembly_level)'
