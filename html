# description
tools for dealing with html (cli)

# filtering
pup - html filtering
fx  - replaces jid and parts of jid (npm; see xx for go version)
htmlq - like JQ for html (homebrew)

# get/post/etc
wget
curl
httpie ( http ) - better curl
