# description
Various notes and tools for mac

# setting defaults
duti

# sys interface
m

# background junk
knockknock.app

# keymaps
karabiner

# uninstall sophos
cd /Library/Application Support/Sophos/opm-sa/Installer.app/Contents/MacOS/tools
sudo ./InstallationDeployer --remove --ui --tamper_password '...'

## uninstall sophos if uninstall fails
sudo /usr/bin/dscl . -delete /Users/_Sophos  #Delete the user
sudo ./InstallationDeployer --remove --ui --tamper_password '...' #Run uninstaller
cp /usr/bin/dscl ~/  #If no prompt says Terminal would like to.... continue with these steps:
sudo ~/dscl . -delete /Users/_Sophos
sudo ./InstallationDeployer --remove --ui --tamper_password '...' #InstallationDeployer must be run as root when the --ui option is not specified.
