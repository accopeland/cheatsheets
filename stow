# description
Stow is a symlink farm manager which takes distinct sets of software and/or
data located in separate directories on the filesystem, and makes them all
appear to be installed in a single directory tree.

# install
brew install stow

# setup

# usage
> mkdir _dotfiles
> cd _dotfiles
> mkdir ncbi
> mv ../.ncbirc ncbi/
> stow -v ncbi
> stow -v sql

# check
